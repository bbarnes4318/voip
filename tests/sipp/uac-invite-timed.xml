<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  uac-invite-timed.xml — the customer's dialer, with the hold time set by -d.

  Sends an INVITE to the SBC, expects the call to be answered end to end
  (which means the SBC built a leg to the FracTEL stub and bridged it), holds
  it, then hangs up cleanly.

  Used by the transfer-leg criteria (19, 20, 21, 23), which need a call of a
  known answered length in order to move the learned ACD across its threshold.

  Injection fields (see destinations.csv):
    [field0] = destination number
    [field1] = caller ID

  Run:
    sipp -sf uac-invite.xml -inf destinations.csv \
         -i 127.0.0.11 -m 1 127.0.0.1:5060

  A timeout waiting for the 200 means the SBC did not complete the call.
  Check, in order: is the FracTEL stub running on 127.0.0.20:5080; did the
  dialplan reject the call (grep SBC-REJECT in /var/log/asterisk/messages);
  is the trunk pointed at the real carrier instead of the stub.
-->

<scenario name="SBC customer leg - answered call, -d controls hold">

  <send retrans="500">
    <![CDATA[

      INVITE sip:[field0]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "[field1]" <sip:[field1]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[field0]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[field1]@[local_ip]:[local_port]>
      Max-Forwards: 70
      User-Agent: SIPp-AcceptanceTest
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8 101
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>

  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[field0]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "[field1]" <sip:[field1]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[field0]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:[field1]@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- NO milliseconds attribute, deliberately. sipp's -d only sets the
       duration of pauses that do not carry one of their own, so this is the
       scenario to use when the test needs a SPECIFIC answered duration.
       uac-invite.xml pauses an explicit 20000ms and -d cannot shorten it.

       Transfer-leg criteria 19, 20 and 21 turn on billsec crossing the ACD
       threshold, so each of them drives this with its own -d. -->
  <pause/>

  <send retrans="500">
    <![CDATA[

      BYE sip:[field0]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "[field1]" <sip:[field1]@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[field0]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="10, 50, 100, 200, 500, 1000"/>
  <CallLengthRepartition value="10, 100, 500, 1000, 5000"/>

</scenario>
