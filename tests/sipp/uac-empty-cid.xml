<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  uac-empty-cid.xml — the empty-caller-ID case.

  This is a separate scenario rather than a row in destinations-badcid.csv on
  purpose. Substituting an empty [field1] into the shared template produces
  From: "" <sip:@host>, which is a malformed SIP URI — PJSIP rejects it at
  the parser with 400 Bad Request before the dialplan ever runs. That would
  "pass" a rejection test for entirely the wrong reason and would tell you
  nothing about whether caller ID validation works.

  What a real dialer with an unset caller ID actually sends is a From header
  with no user part at all, which is well-formed. That is what this sends, so
  the INVITE reaches [sbc-customer], CALLERID(num) is empty, and the caller ID
  check refuses it with 403.

  Run:
    sipp -sf uac-empty-cid.xml -inf destinations.csv \
         -i 127.0.0.11 -m 1 127.0.0.1:5060
-->

<scenario name="Empty caller ID must return 403">

  <send retrans="500">
    <![CDATA[

      INVITE sip:[field0]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[field0]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:[local_ip]:[local_port]>
      Max-Forwards: 70
      User-Agent: SIPp-AcceptanceTest
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=sipp 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0 8
      a=rtpmap:0 PCMU/8000
      a=rtpmap:8 PCMA/8000

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="403" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[field0]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: <sip:[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[field0]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

</scenario>
